-r requirements.txt

# Imported by mcp_dummy/ and the tests; app/ uses httpx2.
httpx==0.28.1

# Geometry for the stand-in MCP server's `compute` tool and deterministic real-GeoParquet
# renderer fixtures. Dev-only: mcp_dummy/ and PyArrow are not in the backend runtime image.
shapely==2.1.2
pyproj==3.7.2
pyarrow==18.1.0

# Independent protobuf decoder for real DuckDB ST_AsMVT integration tests.
mapbox-vector-tile==2.2.0

pytest==9.0.1
pytest-asyncio==1.3.0
ruff==0.15.1
mypy==1.19.0

# Emitted frames are asserted against docs/protocol/*.schema.json, so the schemas
# stay the single source of truth rather than a second copy in the tests.
jsonschema==4.26.0

# Eval question set. Stubs because mypy checks evals/ too.
PyYAML==6.0.3
types-PyYAML==6.0.12.20250915
