# Runtime dependencies for the agent backend. Pinned exactly: the image is the
# only contract with the deployed infrastructure (docs/deployment.md#what-the-container-image-must-provide),
# so a surprise minor bump must not reach production unreviewed.
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
boto3==1.43.59
# Imported directly by app/mcp/client.py for the Streamable HTTP transport, so it is
# pinned here rather than left to arrive transitively via mcp.
httpx2==2.9.1

# MCP 2.0 renamed the client entry point and moved to snake_case model fields;
# it also supersedes FastMCP with MCPServer. Do not "upgrade" from a 1.x recipe.
mcp==2.0.0

# Public Swisstopo exploration MCP, bundled into the existing backend task. The MCP 2
# client uses httpx2, while the geo.admin.ch API wrapper intentionally uses httpx 0.28.
httpx==0.28.1
pyproj==3.7.2
