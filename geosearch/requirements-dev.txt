# Everything the deployed image needs, plus what only a workstation and CI need.
# Install this to develop; the Dockerfile installs requirements.txt alone.
-r requirements.txt

# Same pins as backend/requirements-dev.txt. Two ruff versions in one repository disagree
# about formatting, and whichever ran last looks like the bug. No pytest-asyncio: these
# tests drive the async API through asyncio.run rather than as async test functions.
pytest==9.0.1
ruff==0.15.1
# Shared local S3 endpoint for the separate geosearch and backend processes.
# Production installs requirements.txt only and does not contain moto.
moto[server]==5.2.2
