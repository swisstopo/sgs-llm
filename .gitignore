node_modules/
dist/
.vite/
__pycache__/
*.log

# Python tooling
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Evaluation runs contain model output, are large, and are regenerated on demand.
evals/results/

# Derived by `python -m geosearch.build`: a DuckDB file, the FAISS indexes and a mirror
# of ~2300 boundary GeoJSONs, ~60 MB in total. Rebuildable from swisstopo in one command.
index/

# Working notes kept out of the public repo (*.local alone does not match *.local.md).
*.local.md
mock-agent/feedback.log
.env
.env.*
*.local
*.pem
*.key
*.sqlite3
.DS_Store

# Internal cost analysis — kept local only, not published
docs/model-cost-options/

# Deploy parameters that must not be published (e.g. the developer access IP).
# See infra/dev-access.env.example.
infra/*.local.env
infra/*.params.local.json

# Local credential scratch file — never commit.
.secrets
